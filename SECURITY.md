# 安全说明 / Security

## 支持范围

目前只维护最新的预发布版本。cb 是个人维护的实验性集成，桌面应用或 CLI 更新可能改变其安全与兼容性假设。

## 报告漏洞

优先使用 [GitHub 私密漏洞报告](https://github.com/houhongxu/cpet/security/advisories/new)。如果该入口尚未启用或不可用，请先创建一个不包含利用细节、凭据或私人数据的 issue，请维护者提供私密联系方式；不要公开完整复现材料。维护者会在有能力处理时回复，不承诺服务级别或漏洞奖励。

普通安装失败或宠物未显示请使用 bug 模板。

## 信任边界

- 所有监听地址固定为 `127.0.0.1`。共享 App Server 使用端口 4500，**cb 不为这个固定端口增加鉴权**。同机其他可连接该端口的程序属于需要信任的边界；不要做端口转发或改绑公网/局域网地址。
- 每个 CLI 的临时转接端口使用随机 Bearer 凭据，拒绝带浏览器 Origin 的连接；这不会保护可以直接访问固定共享端口的本机程序。
- 凭据通过 CLI 环境变量传递，不放进 URL 或诊断输出。cb 没有自己的遥测或上传日志功能；模型服务、桌面应用和依赖下载的联网行为由各自组件负责。
- 转接器不改变审批决定或任务输入；任务执行权限由 Codex 后台及用户配置决定。
- 安装器修改用户级别文件，不需要 sudo；会修改 `.zshrc` 的受管块、创建命令链接及运行副本。`enable` 另行添加 LaunchAgent。
- `on` 设置 `CODEX_APP_SERVER_WS_URL`，`off` 尝试恢复之前的值，并保留用户随后自行做出的修改。
- 日志可能包含任务编号、目录、异常信息或应用记录的其他内容。提交诊断前应手动脱敏。
- 源码、依赖环境和运行文件由当前用户控制；源码移动不会影响运行副本，但解释器被卸载仍可能使虚拟环境失效。

不应在共享的不可信 macOS 用户环境中运行未鉴权的共享后台。当前没有远程部署、多租户隔离或权限提升设计。
